package net.radiobalkan.app;

/** Central production metadata used by network clients and UI. */
public final class AppInfo {
    public static final String VERSION = "0.0.40";
    public static final String USER_AGENT = "RadioBalkan-Android/" + VERSION;

    private AppInfo() { }
}
